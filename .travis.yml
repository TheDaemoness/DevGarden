language: cpp
compiler: clang
env:
- DISTRO=ubuntu-vivid

addons:
  apt:
    packages:
    - qt5-qmake
    - qt5-default
    - qtbase5-dev

script: qmake -makefile -o Makefile QMAKE_CXX=$CXX DevGarden.pro && make

after_success: ./install-tools.rb
